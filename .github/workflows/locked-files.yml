name: Locked files

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject changes to locked files
        run: ./scripts/check_locked_files.sh
